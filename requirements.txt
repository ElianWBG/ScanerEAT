# API Django (restaurante_api)
Django>=5.2,<6.0
djangorestframework>=3.15
python-dotenv>=1.0
psycopg2-binary>=2.9   # driver para RDS Postgres en producción
gunicorn>=22.0          # servidor WSGI para EC2/producción

# El microservicio de visión (YOLOv8 + DeepSORT + OpenCV + boto3) vive en su
# propio repo con sus propias dependencias; se comunica con esta API por HTTP.